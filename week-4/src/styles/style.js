import styled from "styled-components";
import { bg, line, sat, sun, today } from "./color.js";

const App = styled.div`
  .calenderTitle {
    text-align: center;
    font-size: 4rem;
    color: ${bg};
  }
`;

export default App;
