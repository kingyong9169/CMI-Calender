import styled from 'styled-components';
import { CSSColors } from './color';

const App = styled.div`
  .calenderTitle {
    text-align: center;
    font-size: 4rem;
    color: ${CSSColors.bg};
  }
`;

export default App;
